@page "/events"
@inject EventService EventService
@inject NavigationManager Navigation

<h2 class="page-title">Upcoming Events</h2>

@if (Events == null)
{
    <p>Loading events...</p>
}
else if (!Events.Any())
{
    <p>No upcoming events.</p>
}
else
{
    <div class="event-grid">
        @foreach (var ev in Events)
        {
            <div class="event-card" @onclick="() => NavigateToDetails(ev.EventId)">
                @* Photos fetched randomly by picsum, you need to change it later *@
                <img class="event-image" src="https://picsum.photos/400/250?random=@ev.EventId"/>

                <div class="event-content">

                    <h3>@ev.EventTitle</h3>

                    <p class="event-date">
                        <i class="fa-solid fa-calendar-day"></i>
                        @ev.StartDate.ToString("dd MMM yyyy - HH:mm")
                    </p>

                    <p class="event-location">
                        <i class="fa-solid fa-location-dot"></i>
                        @ev.Venue.Location.City, @ev.Venue.Location.Country
                    </p>

                    <div class="event-categories">
                        @foreach (var cat in ev.Categories)
                        {
                            <span class="category-chip">@cat.CategoryName</span>
                        }
                    </div>

                    <div class="event-footer">
                        <span class="participants">
                            <i class="fa-solid fa-users"></i>
                            @ev.Enrollments.Count participants
                        </span>
                        
                        <span class="join-status not-joined">Details</span>
                    </div>
                </div>
            </div>
        }
    </div>
}

@code {
    private List<Event> Events;

    protected override async Task OnInitializedAsync()
    {
        Events = await EventService.GetAllEvents();
    }

    void NavigateToDetails(int eventId)
    {
        Navigation.NavigateTo($"/events/{eventId}");
    }
}


@*
@page "/events"
@using Event_Management_System.Models.Base
@using Event_Management_System.Data
@using Microsoft.EntityFrameworkCore
@inject IDbContextFactory<MasDbContext> DbFactory
@inject NavigationManager Nav

<div class="container mt-4">
    <div class="text-center mb-4">
        <h2> Browse Events</h2>
        <input type="text" class="form-control mt-3 w-50 mx-auto" placeholder="🔍 Search events..." />
    </div>

    <div class="row">
        @if (_events is null)
        {
            <p class="text-center">Loading...</p>
        }
        else if (_events.Count == 0)
        {
            <p class="text-center">No events found.</p>
        }
        else
        {
            @foreach (var ev in _events)
            {
                <div class="col-md-4 mb-4">
                    <div class="card shadow-sm h-100 border-0 hover-shadow" style="cursor: pointer;" @onclick="@(() => Nav.NavigateTo($"/events/{ev.EventId}"))">
                        <div class="card-body">
                            <h5 class="card-title fw-bold">@ev.EventTitle</h5>
                            <p class="card-text text-muted">@ev.Description</p>
                        </div>
                        <div class="card-footer bg-light text-muted d-flex align-items-center">
                            <i class="bi bi-calendar-event me-2"></i>
                            @ev.StartDate.ToShortDateString() - @ev.EndDate.ToShortDateString()
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>

<style>
    .hover-shadow:hover {
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        transition: box-shadow 0.3s ease-in-out;
    }

    .card-title {
        font-size: 1.25rem;
        color: #333;
    }

    .card-text {
        font-size: 0.95rem;
    }

    input[type="text"] {
        max-width: 400px;
    }
</style>

@code {
    private List<Event>? _events;

    protected override async Task OnInitializedAsync()
    {
        using var db = await DbFactory.CreateDbContextAsync();

        _events = await db.Events
                          .Include(e => e.Venue)
                              .ThenInclude(v => v.Location)
                          .Include(e => e.Organizer)
                          .OrderBy(e => e.StartDate)
                          .ToListAsync();
    }
}
*@
